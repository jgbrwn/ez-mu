{# Main queue list content #}
{% if jobs is empty %}
<div class="empty-state">
    <div class="empty-state-icon">📥</div>
    <p>No jobs in queue</p>
</div>
{% else %}
{% for job in jobs %}
{% include 'partials/queue_item.twig' with {'job': job} %}
{% endfor %}
{% endif %}

{# Out-of-band updates for stats #}
{% if stats is defined %}
<div id="queue-stats" hx-swap-oob="true">
    {{ stats.queued }} queued • {{ stats.processing }} processing • {{ stats.completed }} completed • {{ stats.failed }} failed
</div>
<span id="queue-badge" hx-swap-oob="true">({{ stats.queued + stats.processing }})</span>
{% endif %}
